def addieren(a: float , b: float) -> int:
    return float(a) + float(b)

def subtrahieren(a: int, b: int) -> int:
    return float(a) - float(b)

def multiplizieren(a: int, b: int) -> int:
    return float(a) * float(b)

def dividieren(a: int, b: int) -> int:
    return float(a) / float(b)
